# Week11 AIと考えたログ

## Part 1：AIと考えた1セッション

### テーマ

JavaScriptのフォームバリデーションで、入力されていない項目にエラーを表示する方法についてAIと考えた。

### 送った指示（良いプロンプトの型）

**目的:**
フォームの未入力項目に、それぞれエラーメッセージを表示できるようにする。

**前提:**
HTMLとJavaScriptを使ってフォームを作成している。`required`や`preventDefault()`などは学習済み。

**見てほしい観点:**
なぜそのコードでエラーを表示できるのか、初心者でも理解できるように説明してほしい。

**制約:**
まだ学習していない難しい書き方はできるだけ使わず、シンプルなJavaScriptで実装する。

**出力形式:**
完成コードだけでなく、処理の流れとコードを書く理由も説明する。

### 返ってきた出力（要約）

AIから、フォームの送信イベントを取得して`preventDefault()`で一度送信を止め、各入力欄の`.value`や`.checked`を確認する方法を提案された。

入力されていない場合は、それぞれの項目に対応したエラーメッセージを表示し、正しく入力されている場合はエラーを消すという処理だった。

### "なぜ"を聞いた記録

* **聞いたこと:**
  なぜ`preventDefault()`を使う必要があるのか。

* **返ってきた説明（要約）:**
  フォームは通常、送信ボタンを押すとページの送信・遷移が行われる。そのままだとJavaScriptで入力内容を確認したり、エラーを表示したりする前に画面が切り替わってしまう場合がある。そのため、`preventDefault()`で標準の送信処理を一度止めてからバリデーションを行う。

* **聞く前は分からなかったが、聞いて分かったこと:**
  最初は「フォーム送信時にとりあえず書くもの」だと思っていたが、フォーム本来の動きを止めて、JavaScript側で入力チェックを行うために必要だと分かった。

### 出力の4分類

| 分類    | 内容                                      | 判断理由                                    |
| ----- | --------------------------------------- | --------------------------------------- |
| 採用する  | `preventDefault()`を使って送信を止めてから入力チェックをする | 理由まで理解でき、今回のフォームに必要だったため                |
| 保留する  | 入力内容をより細かくチェックする処理                      | 今回の課題では未入力チェックを優先したかったため                |
| 採用しない | 学習していない複雑な書き方                           | コードを自分で説明できなくなるため                       |
| 要確認   | `.value`と`.checked`の使い分け                | inputの種類によって取得方法が違うため、自分でも確認する必要があると感じた |

---

## Part 2：流された場面の振り返り

Week09のフォーム作成で、最初はAIが出したコードを動いたからという理由だけでそのまま使ってしまった。特に`preventDefault()`について、なぜ必要なのかを理解しないまま採用していた。

---

## Part 3：今後のAI利用方針

### Week11で気づいたこと

AIのコードは動くかどうかだけで判断するのではなく、「なぜこの処理が必要なのか」を自分で説明できる状態にすることが大切だと気づいた。AIに答えを出してもらうだけではなく、分からない部分を質問することで学習に活用できると思った。

### 自分のAI利用ルール

1. AIが出したコードをそのままコピペせず、分からない部分は「なぜ？」と聞く。
2. 自分で説明できないコードは、理解してから採用する。
3. AIの回答が本当に正しいか、自分のコードで動かしたり資料を確認したりして判断する。
